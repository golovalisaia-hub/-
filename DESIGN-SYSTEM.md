# Academy · дизайн-система

Версия: сентябрь 2026. **Тёмная тема — основная**, светлая включается кнопкой «Тема» в шапке и запоминается в браузере (`data-theme="light"` на `<html>`, `theme.js` выставляет её до первой отрисовки, без вспышки). Вся визуальная основа проекта — в одном файле [`academy-ui.css`](academy-ui.css); прежние
семнадцать таблиц стилей удалены, разметка и скрипты не переписывались. Это **самостоятельный сайт Academy**, а не редизайн планера SEVER.

## Идея

Спокойный тёмный кабинет: глубокий тёмно-синий холст, приподнятые карточки, ещё более тёмные
акцентные панели для карты курса и этапов, лаймовый акцент действия и фиолетовый акцент прогресса.
Светлая тема — те же токены с другими значениями, а не вторая вёрстка. Иерархия ведёт к одному следующему
шагу: продолжить урок, написать ответ, сдать зачёт. Эффекты вторичны по отношению к контрасту и
читаемости при долгом чтении.

Референсы других образовательных платформ — вдохновение для композиции, но не источник чужой
графики, шрифтов и фирменного стиля: иллюстрации и персонаж нарисованы для этого проекта.

## Цветовые токены

| Токен | Тёмная | Светлая | Применение |
| --- | --- | --- | --- |
| `--page` | `#070c13` | `#e9ecf0` | Фон вне рабочей области |
| `--canvas` | `#0b1220` | `#f5f6f8` | Рабочий холст |
| `--card` | `#111c2a` | `#ffffff` | Карточки, панели, поля |
| `--card-2` | `#162333` | `#f2f4f7` | Вложенные блоки, неактивные строки |
| `--dark` | `#060b12` | `#0f1720` | Карта курса, закрытый этап, шапка урока |
| `--line` | `#22344a` | `#e3e7ec` | Границы |
| `--line-strong` | `#33485f` | `#cfd6de` | Границы полей и вторичных кнопок |
| `--text` | `#eaf2f9` | `#101720` | Основной текст |
| `--muted` | `#a2b6c6` | `#5d6b7a` | Подписи и вторичный текст |
| `--muted-2` | `#7d93a6` | `#7b8794` | Третичные подписи |
| `--tint-ok` / `--tint-warn` | `#14291d` / `#2c2413` | `#eefae4` / `#fdf6e6` | Мягкие подложки успеха и архива |
| `--ring-track` | `#1d2b3b` | `#ecf0f4` | Незаполненная часть кольца прогресса |
| `--lime` / `--a-accent` | `#b8f08e` | Основное действие, пройденные узлы карты |
| `--lime-soft` | `#d8ffb8` | Наведение на основном действии |
| `--ink` | `#14210f` | Текст на лаймовых кнопках |
| `--violet` | `#7c5cff` | Прогресс, активный раздел, фокус |
| `--violet-soft` | `#a68aff` | Второй тон прогресса, активный узел карты |
| `--violet-pale` | `#ece7ff` | Подложка активного пункта и статусов |
| `--ok` / `--ok-bg` | `#2f7a4d` / `#eaf7ef` | Подтверждение |
| `--danger` / `--danger-bg` | `#c03a4b` / `#fdeef0` | Ошибка |

Лайм означает действие и успешную стадию, фиолетовый — прогресс и текущее положение. Ошибки и
предупреждения всегда имеют текст, а не только цвет. Новых ярких цветов для QA, Python и English не
вводим. Токен `--a-accent` сохранён под прежним именем: на него опираются автотесты темы.

## Типографика и геометрия

- Системный sans-serif с `Inter` первым в стеке: без внешней зависимости от шрифтов.
- Заголовки плотные (`letter-spacing:-.02em`), основной текст урока с межстрочным интервалом 1.65.
- Моноширинный шрифт — только для кода, ввода и вывода архивной Python-консоли.
- Радиусы: карточки 22 px, вложенные блоки 14 px, поля и кнопки 11 px.
- Тени мягкие и низкоконтрастные; свечение не заменяет границу.
- Любой интерактивный элемент — не меньше 44×44 px, включая `summary`, чекбоксы (целью считается
  подпись) и иконки навигации.
- **У каждой кнопки своя иконка.** Разметка одинакова везде:
  `<button><span class="btn-icon" aria-hidden="true">✓</span><span class="btn-label">Подпись</span></button>`.
  Иконка скрыта от скринридера, подпись остаётся текстом; скрипты меняют только `.btn-label`, поэтому
  иконка не пропадает при смене состояния. Одна иконка = одно действие: ⇥ вход, ⇤ выход, × закрыть,
  ← → соседний шаг, ⇢ следующий урок, ✓ проверить и подтвердить, ★ зачёт, ✎ черновик и заметка,
  ⤓ сохранить в облако, ↻ обновить, ⟲ вернуть исходное, ✚ начать заново, ⚑ подсказка, ⊘ не знаю,
  ♪ ♫ ♬ звук, ▶ запуск упражнения, ⚡ запуск кода, ⧉ копировать, ⌫ удалить, ⇄ перенос правила,
  ⚖ разбор ответа, ⏱ ⏸ ⏹ таймер, ◈ QA, ⌘ Python, ◎ English, ☀ ☾ тема.

## Экраны

### Обзор (`index.html`)

1. Узкий рельс с иконками разделов; подпись появляется при наведении и фокусе.
   Разделы одинаковы на всех страницах: **Обзор · Уроки · Практика · Конспекты**, календарь SEVER —
   отдельной внешней ссылкой. Ни одна страница не придумывает свой набор пунктов и своих названий.
2. Шапка с хлебными крошками и входом, затем строка о том, что аккаунт общий с SEVER.
3. Карта курса: тёмная панель, узлы модулей загораются только от подтверждённого прогресса.
4. Три быстрых перехода: текущий урок, конспекты, календарь SEVER.
5. Модули обучения: две карточки с реальным счётчиком уроков и раскрываемым списком.
6. Закрытый этап 2 и карточка конспектов.
7. Правая колонка: кольцо прогресса, персонаж с достижениями, «что сейчас делать», календарь плана,
   пояснение о трёх стадиях.

### Уроки (`path.html`)

Тёмная шапка занятия, три стадии освоения, теория → пример → практика, два вопроса, поле работы,
критерии и зачёт. Блок зачёта визуально отделён от практики: он не выдаётся за автоматическую оценку
свободного текста.

### Библиотека (`library.html`)

Крупный тёмный герой, форма личного конспекта, боковая карточка с методикой и история записей. Для
гостя запись и изменение заблокированы. Книги целиком не размещаются.

### Практикум, песочница, тренер и ИИ-наставник

`skills.html` — три направления практики, `sandbox.html` — QA-лаборатория с намеренными дефектами,
`mentor.html` — локальное повторение, `ai.html` — разговорный наставник. У всех те же карточки,
кнопки и поля; тёмный герой, белые панели, лаймовое действие.

### Архив (`studio.html`)

Прежние 84 комбинированных урока. Сверху — янтарный баннер «Архив, это не текущий курс». Календарь
отсюда не изменяется. Поле кода, тестовый ввод, кнопка запуска и вывод визуально отличаются от
обычного ответа; на широком экране код и вывод стоят рядом.

## Адаптивность

Единая сетка во всех правилах: **1280** (уплотнение десктопа), **1024** (боковые панели ложатся
сверху), **834** (рельс уходит, появляется закреплённая нижняя навигация), **600** (телефон),
**375** (маленький телефон).

- До 834 px урок идёт перед панелью программы, а навигация превращается в закреплённую панель
  **внизу экрана** — там, куда дотягивается большой палец. Она не перекрывает содержимое: под
  страницей резервируется ровно её высота плюс безопасная зона iPhone
  (`padding-bottom: calc(var(--mobile-nav-h) + env(safe-area-inset-bottom) + 12px)`), а автотест
  прокручивает каждую страницу до конца и проверяет, что панель не накрыла ни одну кнопку.
- Проверочные ширины автотеста: 1440, 1280, 1024, 834, 768, 600, 390, 375, 320 px.
- Для `prefers-reduced-motion: reduce` переходы и анимации отключаются.

## Нельзя сломать при дальнейшем редизайне

- Не изменять репозиторий и код SEVER. Academy пишет только в связанную учебную задачу.
- Не перерисовывать галочку до подтверждения сервера; не трогать другие задачи и пользователей.
- Не терять локальные или облачные черновики при смене урока, вкладки, экрана.
- Не заменять реальный запуск Python заранее подготовленным выводом.
- Не показывать выдуманный прогресс, уровень персонажа или достижения гостю.
- Оставлять работоспособными вход, авторизацию владельца, личный дневник, таймер, переход к
  следующему уроку.
- Любое изменение цвета, оформления или навигации проходит автоматические браузерные проверки на
  большом и маленьком экране.

## Проверка перед релизом

- [x] Единая дизайн-система (тёмная + светлая) подключена ко всем восьми страницам одним файлом.
- [x] Навигация на всех восьми страницах одинакова по составу, порядку, иконкам и подписям.
- [x] Автотест проверяет палитру, отсутствие горизонтального переполнения и тач-таргеты 44×44 на
      девяти ширинах.
- [x] Автотест `tests/interactive.cjs` проходит по всем ссылкам и кнопкам каждой из восьми страниц.
- [x] В изолированном тесте работают вопросы английского, запуск Python, три стадии до зачёта,
      перенос черновика между устройствами и создание/правка/удаление конспекта.
- [ ] Проверить глазами на реальных iPhone, Android и десктопе, включая клавиатуру и системный
      размер шрифта.
- [ ] Проверить реальный авторизованный путь на двух устройствах и публикацию последнего коммита.
- [ ] Отдельно закрыть доступ к сайту перед приватным запуском: публичный GitHub Pages не является
      гарантированно приватным хостингом.

Этот документ фиксирует дизайн и **не обещает несуществующего ИИ-наставника или всех 26 недель
уроков**. Полностью расписаны первые 14 уроков QA и английского; остальное — отдельная задача.
